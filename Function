// Gatilho, oque vai fazer, saída
//Declara uma função

function calcularArea(raio)
{
    const pi=3.14159;
    return pi*raio*raio;
}

let raio=5;
let area = calcularArea(raio);
console.log(`A área do círculo é de ${area} sendo que seu raio é de ${raio}`);